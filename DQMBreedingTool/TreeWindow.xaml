<Window x:Class="DQMBreedingTool.TreeWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:models="clr-namespace:DQMBreedingTool.Models"
        xmlns:conv="clr-namespace:DQMBreedingTool.Converters"
        Title="배합 트리" Height="600" Width="500"
        Background="#1E1E1E"
        WindowStartupLocation="CenterOwner" ShowInTaskbar="True">

    <Window.Resources>
        <conv:NullToVisibilityConverter x:Key="NullToVisibilityConverter"/>
    </Window.Resources>

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TreeView Grid.Row="0" x:Name="RecipeTreeView" Background="Transparent" BorderThickness="0">
            <TreeView.ItemContainerStyle>
                <Style TargetType="TreeViewItem">
                    <Setter Property="IsExpanded" Value="{Binding IsExpanded, Mode=OneWay}" />
                    <Setter Property="Foreground" Value="White" />
                </Style>
            </TreeView.ItemContainerStyle>
            <TreeView.ItemTemplate>
                <HierarchicalDataTemplate DataType="{x:Type models:RecipeNode}" ItemsSource="{Binding Children}">
                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="0,2">
                        <Border Width="28" Height="28" ClipToBounds="True" Margin="0,0,6,0"
                                Visibility="{Binding Icon, Converter={StaticResource NullToVisibilityConverter}}">
                            <Image Source="{Binding Icon}" Width="28" Stretch="UniformToFill" VerticalAlignment="Top" />
                        </Border>
                        <TextBlock Text="{Binding Header}" VerticalAlignment="Center" />
                    </StackPanel>
                </HierarchicalDataTemplate>
            </TreeView.ItemTemplate>
        </TreeView>
    </Grid>
</Window>
